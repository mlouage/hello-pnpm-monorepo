---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<button class:list={["px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600", className]}>
  <slot />
</button>
